# Website Starter

## Quick start

1. Open `index.html` directly in your browser, or
2. Run a local server from this folder:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.

## Next steps

- Replace placeholder text/content.
- Customize colors and typography in `styles.css`.
- Add more sections/components in `index.html`.
